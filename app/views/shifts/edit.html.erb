<%= render 'shared/navbar' %>

<div class="container">
  <% if current_user.admin? %>
    <h1>Edit Shift</h1>
  <% else %>
    <h1>Request Shift</h1>
  <% end %>
<br>

<% if current_user.admin? %>
  <%= render 'form' %>
<% else %>
  <%= render 'request' %>
<% end %>


  <%= link_to 'Back to All Shifts', shifts_path %>
</div>
